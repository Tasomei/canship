node_modules/
dist/
*.log
*.tgz
.DS_Store

# 排除可能包含认证令牌的项目级包管理配置。
.npmrc

# 本地脚本及临时复现目录。
.scratch/

# 文件覆盖前创建的本地备份。
_备份/

# 报告包含问题位置及详情，不应提交；基线需经审阅后单独纳入版本控制。
canship-report.html
canship.sarif

# 夹具中的环境文件使用模拟值，应保留在版本控制中。
.env
.env.local
.env.*.local
!test/fixtures/**/.env*
